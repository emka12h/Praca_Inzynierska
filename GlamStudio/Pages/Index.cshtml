@page
@model IndexModel
@{
    ViewData["Title"] = "Strona główna";
}

<section class="home-section text-center d-flex flex-column align-items-center justify-content-center">
    <div class="container">

        <div class="logo-wrapper mb-5">
            <img src="~/images/logo_menu_nawigacyjne.png" alt="GlamStudio Logo" class="main-logo img-fluid" />
        </div>

        <div class="d-flex justify-content-center align-items-center gap-md-5 flex-wrap">
            <a asp-area="Client" asp-controller="Home" asp-action="Index" class="btn-home">Dla Klientów</a>
            <a asp-area="Identity" asp-page="/Account/Login" asp-route-type="salon" class="btn-home">Panel salonu</a>
        </div>
    </div>
</section>

@section Scripts {
    <script>

    </script>
}
